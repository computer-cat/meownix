#!/usr/bin/env bash

set -uexo pipefail

echo "source /etc/profile.d/brew.sh" | tee -a /etc/bash.bashrc

# Branding variables
IMAGE_NAME="Meownix"
HOSTNAME="meownix"
HOME_URL="https://github.com/computer-cat/meownix"
SUPPORT_URL="https://github.com/computer-cat/meownix"
ID="meownix"
ID_LIKE="arch"
BUILD_ID="rolling"
LOGO=archlinux-logo

# Set default hostname
echo "${HOSTNAME}" > /etc/hostname

# Overwrite os-release
cat > /usr/lib/os-release <<EOF
NAME="${IMAGE_NAME}"
ID="${ID}"
ID_LIKE="${ID_LIKE}"
BUILD_ID="${BUILD_ID}"
PRETTY_NAME="${IMAGE_NAME}"
HOME_URL="${HOME_URL}"
SUPPORT_URL="${SUPPORT_URL}"
LOGO="${LOGO}"
DEFAULT_HOSTNAME="${HOSTNAME}"
IMAGE_VERSION="${IMAGE_VERSION}"
VERSION_ID="${IMAGE_VERSION}"
EOF
